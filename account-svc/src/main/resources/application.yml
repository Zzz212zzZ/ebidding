spring:
  profiles:
    active: test
  application:
    name: account-service

server:
  port: 8001

ebidding:
  account-endpoint: localhost:8001
  bid-endpoint: localhost:8002
  bwic-endpoint: localhost:8003